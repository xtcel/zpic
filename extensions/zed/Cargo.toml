[package]
name = "zpic_assistant"
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/xtcel/zpic"
description = "Zed slash-command integration for the local zpic CLI."

[lib]
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7.0"
